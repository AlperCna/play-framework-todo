# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index()

# Todo CRUD
# HTML formlari sadece GET ve POST destekledigi icin guncelleme/silme de POST kullanir.
GET     /todos                      controllers.TodoController.list()
GET     /todos/new                  controllers.TodoController.createForm()
POST    /todos                      controllers.TodoController.create()
GET     /todos/:id/edit             controllers.TodoController.editForm(id: Long)
POST    /todos/:id                  controllers.TodoController.update(id: Long)
POST    /todos/:id/delete           controllers.TodoController.delete(id: Long)

# ForTry
GET    /fortry/try1                      controllers.ForTryController.try1()
GET    /fortry/try2                      controllers.ForTryController.try2()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
