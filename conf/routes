# Mona DRP — Routes

GET  /         drp.shared.web.HomeController.index()
GET  /health   drp.shared.web.HealthController.health()

# Static assets
GET  /assets/*file   controllers.Assets.versioned(path="/public", file: Asset)

# --- Todo referans kodu (devre disi) ---
# Asagidaki route'lar kasitli olarak yoruma alinmistir.
# app/todo/ kodu derlenmeye devam eder ancak hicbir endpoint'e baglanmaz.
